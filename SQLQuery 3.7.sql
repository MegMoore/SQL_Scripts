--ch 3.7 Write a select statement that determine the paymentdate column...
SELECT PaymentDate
FROM Invoices
WHERE InvoiceTotal [not] null
        SELECT *
        FROM NullSample
        WHERE invoiceTotal
